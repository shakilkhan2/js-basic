const numbers = [12, 34, 21, 45, 87, 55, 63];
for(i = 0; i < numbers.length; i++){
    console.log(i, numbers[i]);
}

const items = ["tab", "mouse", "laptop", "keyboard", "speaker", "monitor"];
for(i = 0; i < items.length; i++){
    let item = items[i];
    if(item == "speaker"){
        break;
    }
    
}