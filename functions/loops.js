const arr = [1, 25, 5, 4, 90];

// Initialize sum outside the loop
let sum = 0;

for (let i = 0; i < arr.length; i++) {
    // Add the current element to the sum
    sum += arr[i];

    // Print the cumulative sum
    
}
console.log(sum);